import styled from "styled-components";

export const LoginWrapper = styled.div`
  width: 75%;
  margin: auto;

  .create-account-link {
    display: block;
    text-align: center;
    margin-bottom: 1em;
  }

  .login-button {
    display: block;
    position: relative;
    left: 50%;
    transform: translateX(-50%);
    width: 250px;
    height: 50px;
  }
`;
